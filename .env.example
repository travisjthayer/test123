# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# CORS Configuration (optional)
# Set to specific domain in production, e.g., http://your-ec2-public-ip
# or leave blank to allow all origins
CORS_ORIGIN=

# EC2 Deployment Notes:
# 1. Copy this file to .env and update with your actual values
# 2. Ensure PostgreSQL is installed and accessible
# 3. Open port 3000 (or your chosen PORT) in EC2 Security Group
# 4. For production, consider using a reverse proxy (nginx) and PM2
# 5. Set NODE_ENV=production for production deployments